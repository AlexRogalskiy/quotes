name: Validate JSONs

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  verify-json-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      ## ${{ steps.json_data.outputs.content }}
#      - name: Create JSONs list
#        id: json_data
#        run: echo "::set-output name=content::$(ls -p ./data | grep -v / | tr '\n' ',')"

      - name: Validate JSON
        uses: docker://orrosenblatt/validate-json-action:latest
        env:
          INPUT_SCHEMA: ./data/schemas/schema.json
          INPUT_JSONS: ./data/anger_quotes.json,./data/books_quotes.json
